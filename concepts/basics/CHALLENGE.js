// 1. Fill in The Blanks with suitable variable values
const isBoy = true  // Hint ( Use Boolean )
const bestJavascriptCourse = "js by Shlok" // Hint ( Use String ), write name
const javascriptLevel = 3 // Hint ( Use Number ) 
const myHobbies = ["exercise", "cycling" , "listeningmusic"] // use array to list your all hobbies

// 2. Fix Following Variables Name 

const evenNumber = 19
let ChannelName = "Do Some Coding" // Hint String
const DidYouSubscribedChannel = true; // Hint Boolean
let mySchoolOrCollageName = "xyz"; // hint write down your collage / school name
const a = 3.14 // 3.14 value of pie 
const c  = "helllo" // hint - reserved keyword


// 3 Answer Following ? 
const myName = "anshu raj"
myName = "Rohan" 
console.log(myName)
// what will be value of myName ?
//anshu raj
// Can you assign Rohan to my Name -> Explain 
// we can't assign because const myName can't be reinitialized
//4. Create an simple object → Object Name Should be mySchoolData 
//  following should be keys name,roll,class,friends

let mySchoolData = {
    name : "fps",
    roll : 23,
    class: 10,
    friends: "lol"
}